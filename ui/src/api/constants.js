export const localhost = `${window.location.origin}/api`;

export const invoiceEndpoint = `${localhost}/invoice`;
export const clientEndpoint = `${localhost}/client`;
export const PDFEndpoint = `${localhost}/pdf`;
export const registerEndpoint = `${localhost}/register`;
export const loginEndpoint = `${localhost}/login`;
export const userEndpoint = `${localhost}/user`;
